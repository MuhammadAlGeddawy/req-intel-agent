cd /app
python -c "
import requests
from pathlib import Path
path = Path('sample_requirements.txt')  # Use /app/sample_requirements.txt
payload = {'document': path.read_text(encoding='utf-8'), 'document_name': path.name}
print('Sending request...')
response = requests.post('http://127.0.0.1:8000/analyze', json=payload, timeout=300)
print(response.status_code, response.json() if response.ok else response.text)
"
