# Copy this file to config/.env and fill in your actual values.
# DO NOT commit config/.env to version control.

# OpenRouter or compatible LLM API key
OPENROUTER_API_KEY=your_key_here

# SQLite database URL (container path; should match the volume mount in docker-compose.yaml)
DATABASE_URL=sqlite:////app/data/requirements_agent.db

